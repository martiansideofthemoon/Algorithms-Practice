#include <iostream>
#include <climits>
using namespace std;
int main()
{
	int test;
	cin >> test;
	for (int i = 0; i < test; ++i)
	{
		int m,n;
		cin >> m >> n;
		int weights[m][n];
		for (int j=0;j<m;j++)
		{
			for (int k=0;k<n;k++)
				cin >> weights[i][j];
		}
		int max=INT_MIN;
	}
}